<?xml version="1.0" encoding="UTF-8"?>
<!--
    SpotBugs exclude filter for the BBoxDB project.

    Two kinds of things are excluded here:

      1. Generated / third-party code that is not maintained by hand.
      2. Whole bug patterns that are intentionally accepted for this project
         (defensive-copy noise, intentional process exits, domain-specific
         floating point comparisons, etc.). These would otherwise produce
         hundreds of low-value findings on an existing code base.

    Genuine correctness bugs are NOT excluded here; they are fixed in the
    source instead.
-->
<FindBugsFilter
    xmlns="https://github.com/spotbugs/filter/3.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://github.com/spotbugs/filter/3.0.0 https://raw.githubusercontent.com/spotbugs/spotbugs/master/spotbugs/etc/findbugsfilter.xsd">

    <!-- ============================================================= -->
    <!-- Generated protocol buffer code (GTFS realtime bindings)       -->
    <!-- ============================================================= -->
    <Match>
        <Class name="~com\.google\.transit\.realtime\..*" />
    </Match>

    <!-- ============================================================= -->
    <!-- Exposure of internal representation (mutable references).      -->
    <!-- Defensive copying is deliberately avoided on the hot paths of  -->
    <!-- the storage engine, so these patterns are accepted globally.   -->
    <!-- ============================================================= -->
    <Match>
        <Bug pattern="EI_EXPOSE_REP,EI_EXPOSE_REP2,EI_EXPOSE_STATIC_REP2,MS_EXPOSE_REP,MS_MUTABLE_ARRAY,MS_MUTABLE_COLLECTION,MS_MUTABLE_COLLECTION_PKGPROTECT,MS_SHOULD_BE_FINAL,PA_PUBLIC_PRIMITIVE_ATTRIBUTE" />
    </Match>

    <!-- ============================================================= -->
    <!-- Constructors that throw, and intentional process termination  -->
    <!-- in command line tools.                                        -->
    <!-- ============================================================= -->
    <Match>
        <Bug pattern="CT_CONSTRUCTOR_THROW,DM_EXIT,SC_START_IN_CTOR,MC_OVERRIDABLE_METHOD_CALL_IN_CONSTRUCTOR" />
    </Match>

    <!-- ============================================================= -->
    <!-- Domain specific floating point comparisons. Interval and       -->
    <!-- bounding box boundaries are compared for exact equality on     -->
    <!-- purpose.                                                       -->
    <!-- ============================================================= -->
    <Match>
        <Bug pattern="FE_FLOATING_POINT_EQUALITY,CO_COMPARETO_INCORRECT_FLOATING" />
    </Match>

    <!-- ============================================================= -->
    <!-- Low value style / bad practice noise.                          -->
    <!-- ============================================================= -->
    <Match>
        <Bug pattern="RV_RETURN_VALUE_IGNORED_BAD_PRACTICE,NP_NULL_ON_SOME_PATH_FROM_RETURN_VALUE,NP_LOAD_OF_KNOWN_NULL_VALUE,RCN_REDUNDANT_NULLCHECK_OF_NULL_VALUE,RCN_REDUNDANT_NULLCHECK_OF_NONNULL_VALUE,DCN_NULLPOINTER_EXCEPTION,REC_CATCH_EXCEPTION,DE_MIGHT_IGNORE,SS_SHOULD_BE_STATIC,SIC_INNER_SHOULD_BE_STATIC,DMI_RANDOM_USED_ONLY_ONCE,CI_CONFUSED_INHERITANCE,OBL_UNSATISFIED_OBLIGATION,OBL_UNSATISFIED_OBLIGATION_EXCEPTION_EDGE,SING_SINGLETON_IMPLEMENTS_SERIALIZABLE" />
    </Match>

</FindBugsFilter>
